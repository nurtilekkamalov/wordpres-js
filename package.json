{
  "name": "wordpres-js",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "$(npm bin)/faust dev",
    "build": "$(npm bin)/faust build",
    "generate": "$(npm bin)/faust generate",
    "start": "$(npm bin)/faust start"
  },
  "dependencies": {
    "@apollo/client": "^3.8.4",
    "@faustwp/core": "^1.2.0",
    "graphql": "^16.8.1",
    "next": "14.0.3",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
